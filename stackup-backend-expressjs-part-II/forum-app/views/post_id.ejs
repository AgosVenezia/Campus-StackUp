<!DOCTYPE html>
<html>

<head>
    <title>Forum</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
</head>

<body>
    <%- include('components/header', {username: username ? username: "" }) %>
        <div style="display: flex; align-items: columns; justify-content: center;">
            <div style="padding: 10px; background-color: white; max-width: 1000px; width: 100%; margin-top: 10px;">
                <div style="border: 1px solid gray; padding-left: 10px; padding-right: 10px;">
                    <h4>
                        <%= post.title %>
                    </h4>
                    <p>
                        <%= post.content %>
                    </p>
                </div>
                <!-- Form to post a new comment -->
                <form action="/post/comment/id/<%= post._id %>" method="post">
                    <label for="title">Comment:</label>
                    <textarea id="comment" name="comment" style="width: 100%; min-height: 100px; padding: 10px 20px;"
                        required></textarea>
                    <button type="submit" style="margin-top: 10px;">Post Comment</button>
                </form>
                <!-- Comments -->
                <div style="margin-top: 10px;">
                    <% comments?.forEach(comment=>{ %>
                        <div class="comment">
                            <p class="comment-p">
                                <%= comment.comment %>
                            </p>
                            <% }) %>
                        </div>
                </div>
            </div>
</body>

</html>